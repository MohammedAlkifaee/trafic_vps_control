<?xml version="1.0" encoding="utf-8"?>
<!--
  Configures the app-blocking accessibility service. We only need to know which
  window/app comes to the foreground, so we listen for window-state changes and
  do NOT request window-content retrieval (canRetrieveWindowContent="false"),
  which keeps the permission footprint minimal.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_description"
    android:notificationTimeout="100" />
